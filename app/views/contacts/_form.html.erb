<%= form_for(@contact, html: {class: 'col-md-10'}) do |f| %>
<% if @contact.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@contact.errors.count, "error") %>
    prohibited this contact from being saved:</h2>

    <ul>
      <% @contact.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field form-group">
    <%#= f.label :subject, class: 'col-md-3 control-label' %>
    <!-- <div class="col-md-6"> -->
    <%= f.text_field :subject, class: 'form-control', :placeholder => "Insert your Subject" %>
    <!-- </div> -->
  </div>

  <div class="field form-group">
    <!-- <%= f.label :content, class: 'col-md-3 control-label' %> -->
    <!-- <div class="col-md-6"> -->
    <%= f.text_area :content, class: 'form-control', rows: '10', :placeholder => "Insert a Comment" %>
    <!-- </div> -->
  </div>

  <br>
  <div class="actions">
    <!-- <div class="col-md-offset-3 col-md-6"> -->
    <%= f.submit "Add a Comment", class: "btn btn-primary btn-block" %>
    <!-- </div> -->
  </div>
  <% end %>
